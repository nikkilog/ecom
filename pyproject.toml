[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "console-core"
version = "0.1.0"
description = "Shared public boundaries for Console Core"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["console_core*"]
